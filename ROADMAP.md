# 打击感 Roadmap

目标：把俄罗斯方块做出动作游戏的打击感。核心不是特效多，而是**重量感**——
冲击瞬间世界停一下、镜头砸一下、碎屑飞一下。

## 已有基础（v0.1）

4x2 像素/格的象限画布（`Canvas` + 半块双色调色）、`game.rs` 特效状态机
（`tick_effects` 驱动）、斜面砖块、ghost 点阵、消行白光烧穿、硬降拖影、
锁定白闪、震屏、飘分、READY/PAUSED 呼吸脉动。渲染固定 ~30fps。

## Batch 1 —— 砸下去的手感

按性价比排序，1+2+3+4 叠加即为目标手感；6+8 顺手带上。

| # | 技巧 | 动作游戏原型 | 实现方案 | 状态 |
|---|------|------------|---------|------|
| 1 | Hit Stop 帧冻结 | 怪猎重击定帧 | 硬降/消行瞬间 `hitstop: Option<Duration>`（60~100ms），期间冻结重力与出块 | [ ] |
| 2 | 粒子 | 打击火花 + 落地尘土 | `Vec<Particle{x,y,vx,vy,life}>`：消行迸火花（抛物线飞散），硬降落点向两侧弹尘土；每帧积分，画布直画像素 | [ ] |
| 3 | 音效 | 打击音=打击感另一半 | 零依赖：`afplay /System/Library/Sounds/*.aiff` 后台 fire-and-forget。硬降=`Funk`、消行=`Glass`、Tetris=`Hero`、升级=`Sosumi`、暂停=`Pop`、结束=`Basso`；缺二进制/非 macOS 静默降级 | [ ] |
| 4 | 镜头下坠 recoil | 重击镜头下沉回弹 | 硬降瞬间整盘 blit 下移 1px 停 2 帧再弹回。下沉=重量，随机抖动=混乱，两者互补 | [ ] |
| 5 | 帧率升 60fps | 顺滑动画基底 | `FRAME` 33ms→16ms；重力走 `Instant` 计时不受影响，粒子/震屏立刻顺滑 | [ ] |
| 6 | 缓动曲线 | 拳拳到肉的节奏 | 飘分/拖影/呼吸由线性 t 换 ease-out（前快后慢），飘分先弹后飘 | [ ] |

## Batch 2 —— 连段与形变

| # | 技巧 | 动作游戏原型 | 实现方案 | 状态 |
|---|------|------------|---------|------|
| 7 | 挤压变形 squash | 落地压扁回弹 | 锁定瞬间该块压扁 1 像素（2px 高→1px、4px 宽→5px），2~3 帧恢复 | [ ] |
| 8 | 连击递进 | 连段音调升高 | 连续消行计数：combo 越高震屏越强、白闪越亮、飘分带 `COMBO ×N`、音效音调递进 | [ ] |
| 9 | 移动手感 DAS/ARR | 输入缓冲 | 按住 h/l 自动移位（DAS ~150ms 后 ARR ~40ms），不再依赖终端按键重复 | [ ] |

## 原则

- 一切动画由 `game.rs` 特效状态机出数据、`ui.rs` 画布消费，状态可测试。
- 零新增依赖：音效走系统命令，粒子/缓动手写。
- 特效必须有寿命上限（现有 `tick_effects` 过期即清），防刷屏。
